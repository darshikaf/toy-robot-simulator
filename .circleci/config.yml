version: 2.1
jobs:
  build_test:
    docker:
      - image: cimg/base:2021.04
    steps:
      - checkout  # checkout source code to working directory
      - run:
          command: |  # create whl and use pipenv to install dependencies
            make test
            make test-python-lint
            make test-python-types

workflows:
  build_test_publish:
    jobs:
      - build_test
